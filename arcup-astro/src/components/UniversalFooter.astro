---
const withBase = (path: string) => {
  const base = import.meta.env.BASE_URL || '/';
  if (path === '/') return base;
  return `${base}${path.replace(/^\//, '')}`;
};
---

<footer class="universal-footer">
  <div class="footer-container">
    <!-- Header Section -->
    <div class="footer-header">
      <h2 class="footer-brand">arc^ by ali</h2>
      <p class="footer-tagline">Open plasma research for regenerative futures</p>
    </div>

    <!-- Main Footer Content -->
    <div class="footer-content">
      <!-- Framework Section -->
      <div class="footer-section">
        <h3 class="footer-section-title">Framework</h3>
        <ul class="footer-links">
          <li><a href={withBase("/why")}>Worldview</a></li>
          <li><a href={withBase("/how")}>Principles</a></li>
          <li><a href={withBase("/how")}>RE:TECH</a></li>
          <li><a href={withBase("/how")}>Method</a></li>
        </ul>
      </div>

      <!-- Legal Section -->
      <div class="footer-section">
        <h3 class="footer-section-title">Legal</h3>
        <ul class="footer-links">
          <li><a href={withBase("/how")}>Licensing</a></li>
          <li><a href={withBase("/docs/terms-of-use")}>Terms of Use</a></li>
          <li><a href={withBase("/docs/privacy-policy")}>Privacy</a></li>
          <li><a href={withBase("/docs/code-of-conduct")}>Code of Conduct</a></li>
          <li><a href={withBase("/how")}>Safety</a></li>
        </ul>
      </div>

      <!-- Community Section -->
      <div class="footer-section">
        <h3 class="footer-section-title">Community</h3>
        <ul class="footer-links">
          <li><a href="https://github.com/arcup" target="_blank" rel="noopener noreferrer">GitHub</a></li>
          <li><a href="https://discord.gg/arcup" target="_blank" rel="noopener noreferrer">Discussions</a></li>
          <li><a href="https://docs.google.com/forms/d/e/1FAIpQLScyVWKDifYxaVWOhdW-SPEXXDVEjBmVezv7W668lizvRCQuaQ/viewform?usp=header" target="_blank" rel="noopener noreferrer">Contact</a></li>
          <li><a href={withBase("/join")}>Contribute</a></li>
          <li><a href={withBase("/docs/research-log")}>Research Log</a></li>
        </ul>
      </div>

      <!-- Social Links Section -->
      <div class="footer-section">
        <h3 class="footer-section-title">Connect</h3>
        <div class="social-links">
          <a href="https://www.youtube.com/@arc-arcup" target="_blank" rel="noopener noreferrer" class="social-link" aria-label="YouTube">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M22.54 6.42a2.78 2.78 0 0 0-1.94-2C18.88 4 12 4 12 4s-6.88 0-8.6.46a2.78 2.78 0 0 0-1.94 2A29 29 0 0 0 1 11.75a29 29 0 0 0 .46 5.33A2.78 2.78 0 0 0 3.4 19c1.72.46 8.6.46 8.6.46s6.88 0 8.6-.46a2.78 2.78 0 0 0 1.94-2 29 29 0 0 0 .46-5.25 29 29 0 0 0-.46-5.33z"/>
              <polygon points="9.75,15.02 15.5,11.75 9.75,8.48"/>
            </svg>
            <span>YouTube</span>
          </a>
          
          <a href="https://discord.gg/DA8BPA3VsN" target="_blank" rel="noopener noreferrer" class="social-link" aria-label="Discord">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/>
              <path d="M13 8H7"/>
              <path d="M17 12H7"/>
            </svg>
            <span>Discord</span>
          </a>
          
          <a href="https://linkedin.com/in/yourprofile" target="_blank" rel="noopener noreferrer" class="social-link" aria-label="LinkedIn">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"/>
              <rect x="2" y="9" width="4" height="12"/>
              <circle cx="4" cy="4" r="2"/>
            </svg>
            <span>LinkedIn</span>
          </a>
          
          <a href="mailto:alixarcup@gmail.com" class="social-link" aria-label="Email">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
              <polyline points="22,6 12,13 2,6"/>
            </svg>
            <span>Email</span>
          </a>
        </div>
      </div>
    </div>

    <!-- Footer Bottom -->
    <div class="footer-bottom">
      <div class="footer-bottom-content">
        <p class="footer-copyright">
          © 2024 arc^ by ali. All content licensed under 
          <a href="https://creativecommons.org/licenses/by-nc-sa/4.0/" target="_blank" rel="noopener noreferrer">CC BY-NC-SA 4.0</a>
        </p>
        <div class="footer-actions">
          <button type="button" data-motion-toggle class="button button-muted">Disable Motion</button>
        </div>
      </div>
    </div>
  </div>
</footer>

<style>
  .universal-footer {
    margin-top: clamp(3rem, 7vw, 5rem);
    background: linear-gradient(135deg, rgba(6, 12, 22, 0.8), rgba(6, 12, 22, 0.95));
    border-top: 1px solid rgba(93, 225, 228, 0.2);
    backdrop-filter: blur(20px);
  }

  .footer-container {
    width: var(--shell-width);
    margin: 0 auto;
    padding: clamp(2.5rem, 6vw, 4rem) clamp(1.5rem, 4vw, 2.5rem);
  }

  /* Header Section */
  .footer-header {
    text-align: center;
    margin-bottom: clamp(2.5rem, 6vw, 3.5rem);
    padding-bottom: clamp(1.5rem, 4vw, 2rem);
    border-bottom: 1px solid rgba(93, 225, 228, 0.15);
  }

  .footer-brand {
    margin: 0 0 0.5rem 0;
    font-size: clamp(1.8rem, 4.5vw, 2.4rem);
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: lowercase;
    color: var(--color-text);
  }

  .footer-tagline {
    margin: 0;
    font-size: clamp(1rem, 2.5vw, 1.15rem);
    color: var(--color-text-muted);
    line-height: 1.6;
  }

  /* Main Content */
  .footer-content {
    display: grid;
    gap: clamp(2rem, 5vw, 3rem);
    grid-template-columns: repeat(auto-fit, minmax(min(200px, 100%), 1fr));
    margin-bottom: clamp(2rem, 5vw, 3rem);
  }

  @media (min-width: 768px) {
    .footer-content {
      grid-template-columns: repeat(4, 1fr);
    }
  }

  .footer-section {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .footer-section-title {
    margin: 0;
    font-size: clamp(1.1rem, 2.8vw, 1.25rem);
    font-weight: 600;
    color: var(--color-primary-bright);
    letter-spacing: 0.05em;
    text-transform: uppercase;
  }

  .footer-links {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .footer-links a {
    color: var(--color-text-muted);
    text-decoration: none;
    font-size: clamp(0.95rem, 2.3vw, 1.05rem);
    line-height: 1.5;
    transition: all var(--transition-base);
    position: relative;
    padding-left: 0;
  }

  .footer-links a:hover {
    color: var(--color-primary-bright);
    transform: translateX(4px);
  }

  .footer-links a.coming-soon {
    opacity: 0.6;
    cursor: not-allowed;
  }

  .footer-links a.coming-soon:hover {
    transform: none;
    color: var(--color-text-muted);
  }

  /* Social Links */
  .social-links {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .social-link {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    color: var(--color-text-muted);
    text-decoration: none;
    font-size: clamp(0.95rem, 2.3vw, 1.05rem);
    line-height: 1.5;
    transition: all var(--transition-base);
    padding: 0.5rem 0;
  }

  .social-link:hover {
    color: var(--color-primary-bright);
    transform: translateX(4px);
  }

  .social-link svg {
    flex-shrink: 0;
    opacity: 0.8;
  }

  .social-link:hover svg {
    opacity: 1;
  }

  /* Footer Bottom */
  .footer-bottom {
    border-top: 1px solid rgba(93, 225, 228, 0.15);
    padding-top: clamp(1.5rem, 4vw, 2rem);
  }

  .footer-bottom-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .footer-copyright {
    margin: 0;
    font-size: clamp(0.85rem, 2vw, 0.95rem);
    color: var(--color-text-muted);
    line-height: 1.6;
  }

  .footer-copyright a {
    color: var(--color-primary-bright);
    text-decoration: underline;
    text-decoration-color: rgba(93, 225, 228, 0.3);
    transition: text-decoration-color var(--transition-base);
  }

  .footer-copyright a:hover {
    text-decoration-color: var(--color-primary-bright);
  }

  .footer-actions {
    display: flex;
    gap: 1rem;
  }

  /* Mobile Responsive */
  @media (max-width: 640px) {
    .footer-container {
      padding: 2rem 1.5rem;
    }

    .footer-content {
      grid-template-columns: repeat(2, 1fr);
      gap: 2rem;
    }

    .footer-bottom-content {
      flex-direction: column;
      text-align: center;
      gap: 1.5rem;
    }

    .footer-actions {
      justify-content: center;
    }
  }

  @media (max-width: 480px) {
    .footer-content {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }
  }

  /* Accessibility */
  @media (prefers-reduced-motion: reduce) {
    .footer-links a:hover,
    .social-link:hover {
      transform: none;
    }
  }
</style>

